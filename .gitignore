# Binaries
bin/
axen
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (since we're using Go modules, vendor is sometimes committed, but let's ignore it by default)
vendor/

# Go workspace file
go.work
go.work.sum

# OS specific files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDEs
.idea/
.vscode/
*.swp
*.swo

# Project specific temporary files
tmp/
coverage.txt
